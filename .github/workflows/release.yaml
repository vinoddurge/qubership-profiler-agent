name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        required: false
        type: string
        default: ''
        description: Release version number (without v, defaults to gradle.properties)

permissions:
  contents: read

jobs:
  publish-release:
    name: Publish release
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.release_version.outputs.version }}
    permissions:
      # write permission is required to create a GitHub release
      contents: write
      # write permission is required for autolabeler
      # otherwise, read permission is required at least
      pull-requests: read
    steps:
      - name: Validate input parameters
        env:
          VERSION: ${{ inputs.version }}
        # language=bash
        run: |
          # Ensure we release from main or release/** branches, ensure version number follows semver
          BRANCH_NAME=${GITHUB_REF##refs/heads/}
          if [[ ! "$BRANCH_NAME" =~ ^(main|release/.+)$ ]]; then
            echo "Error: Release must be created from 'main' or 'release/**' branch. Current branch is '$BRANCH_NAME'"
            exit 1
          fi

          # Validate version follows semver or is empty
          if [[ -n "$VERSION" && ! "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Error: Version must be blank or follow semantic versioning format (x.y.z). Got '$VERSION'"
            exit 1
          fi

      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Get the release version
        id: release_version
        shell: bash
        env:
          VERSION: ${{ inputs.version }}
        # language=bash
        run: |
          if [[ -n "$VERSION" ]]; then
            echo "Got release version from workflow input: $VERSION"
          else
            VERSION=$(grep "^profiler.version=" gradle.properties | cut -d'=' -f2)
            echo "Got release version from gradle.properties: $VERSION"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Check if tag exists
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8.0.0
        env:
          VERSION: ${{ steps.release_version.outputs.version }}
        with:
          # language=javascript
          script: |
             const tag = `v${process.env.VERSION}`;
             // Check if the tag exists
             const { data: tags } = await github.rest.git.listMatchingRefs({
                 owner: context.repo.owner,
                 repo: context.repo.repo,
                 ref: `tags/${tag}`
             });
             if (tags.length > 0) {
                 core.setFailed(`Tag ${tag} already exists. Bump version in gradle.properties and try releasing again.`);
             }

      - name: Prepare git name and email
        # language=bash
        run: |
          # Configure git user.name and user.email
          # See https://github.com/actions/checkout?tab=readme-ov-file#push-a-commit-using-the-built-in-token
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - uses: actions/create-github-app-token@fee1f7d63c2ff003460e3d139729b119787bc349 # v2.2.2
        id: app_token
        with:
          app-id: ${{ vars.GH_BUMP_VERSION_APP_ID }}
          private-key: ${{ secrets.GH_BUMP_VERSION_APP_KEY }}

      - name: Update version in gradle.properties to ${{ steps.release_version.outputs.version }}
        uses: ./.github/actions/update_version
        with:
          token: ${{ steps.app_token.outputs.token }}
          version: ${{ steps.release_version.outputs.version }}

      - name: Set up JDK 21
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5.2.0
        with:
          distribution: zulu
          java-version: 21
          server-id: central

      - name: Setup Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version-file: diagtools/go.mod
          # Disable Go build cache in the release workflow to avoid a
          # cache-poisoning vector (zizmor cache-poisoning): cached Go
          # modules/build outputs could influence the binaries shipped
          # by release-drafter below.
          cache: false

      - name: Setup Zig
        # Zig is used as the cgo C compiler for cross-compiling diagtools
        # (linux/amd64, linux/arm64, windows/amd64) — see diagtools/Makefile.
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2.2.1
        with:
          version: 0.15.2
          # Disable the zig-cache in the release workflow for the same
          # cache-poisoning reason as actions/setup-go above — cached
          # lazy-built musl/compiler-rt artifacts could influence the
          # binaries shipped by release-drafter.
          use-cache: false

      # Publish to Central before generating a tag, so we don't need to drop the tag if
      # Central deployment fails.
      - name: Publish to Central Portal
        uses: burrunan/gradle-cache-action@663fbad34e03c8f12b27f4999ac46e3d90f87eca # v3.0.1
        with:
          arguments: publishAggregationToCentralPortal
          # language=properties
          properties: |
            release=true
            centralPortalPublishingType=AUTOMATIC
        env:
          CENTRAL_PORTAL_USERNAME: ${{ secrets.MAVEN_USER }}
          CENTRAL_PORTAL_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
          SIGNING_PGP_PRIVATE_KEY: ${{ secrets.MAVEN_GPG_PRIVATE_KEY }}
          SIGNING_PGP_PASSPHRASE: ${{ secrets.MAVEN_GPG_PASSPHRASE }}

      - name: Publish GitHub release
        uses: release-drafter/release-drafter@6a93d829887aa2e0748befe2e808c66c0ec6e4c7 # v6.4.0
        id: publish_release
        with:
          disable-autolabeler: true
          publish: true
          latest: ${{ github.ref_name == github.event.repository.default_branch }}
          version: ${{ steps.release_version.outputs.version }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute the next patch version
        id: next_version
        env:
          RELEASE_VERSION: ${{ steps.release_version.outputs.version }}
        # language=bash
        run: |
          # Compute the next patch version
          IFS='.' read -r major minor patch <<< "$RELEASE_VERSION"
          next_version="${major}.${minor}.$((patch + 1))"
          echo "Next version: $next_version"
          echo "version=$next_version" >> "$GITHUB_OUTPUT"

      - name: Update version in gradle.properties to ${{ steps.next_version.outputs.version }}
        uses: ./.github/actions/update_version
        with:
          token: ${{ steps.app_token.outputs.token }}
          version: ${{ steps.next_version.outputs.version }}

  load-docker-components:
    name: Load Docker Build Components
    needs: [publish-release]
    runs-on: ubuntu-latest
    outputs:
      components: ${{ steps.load.outputs.components }}
      platforms: ${{ steps.load.outputs.platforms }}
    env:
      CONFIG_FILE: backend/.qubership/docker-build-config.cfg
    steps:
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: 'v${{ needs.publish-release.outputs.version }}'
          persist-credentials: false

      - name: Load Docker Configuration
        id: load
        env:
          CONFIG_PATH: backend/.qubership/docker-build-config.cfg
        # language=bash
        run: |
          verify=$(cat "$GITHUB_WORKSPACE/${CONFIG_PATH}" | jq '
          def verify_structure:
          .components as $components
          | .platforms as $platforms
          | ($components | type == "array")
          and (all($components[]; has("name") and has("file") and has("context")))
          and ($platforms | type == "string");
          verify_structure
          | if . then true else false end
          ')
          if [ "${verify}" == 'true' ]; then
            echo "✅ Docker build config is valid"
            components=$(jq -c ".components" "$GITHUB_WORKSPACE/${CONFIG_PATH}")
            platforms=$(jq -c ".platforms" "$GITHUB_WORKSPACE/${CONFIG_PATH}")
          else
            echo "❗ Docker build config is invalid"
            exit 1
          fi
          echo "components=${components}" >> $GITHUB_OUTPUT
          echo "platforms=${platforms}" >> $GITHUB_OUTPUT

  docker-build:
    name: "Docker: ${{ matrix.component.name }}"
    needs: [publish-release, load-docker-components]
    permissions:
      contents: read
      packages: write
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        component: ${{ fromJson(needs.load-docker-components.outputs.components) }}
    steps:
      - name: Docker build and publish
        uses: netcracker/qubership-workflow-hub/actions/docker-action@6a2de8d62ac5b6998e30f923613c998c263ea9f2 # v2.2.2
        with:
          ref: 'v${{ needs.publish-release.outputs.version }}'
          download-artifact: false
          dry-run: false
          component: ${{ toJson(matrix.component) }}
          platforms: ${{ needs.load-docker-components.outputs.platforms }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
